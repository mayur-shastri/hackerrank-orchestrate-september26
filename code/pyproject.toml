toml
[project]
name = "buy-or-wait"
version = "0.1.0"
description = "AI-powered financial affordability agent for the HackerRank Orchestrate hackathon"
requires-python = ">=3.11"

dependencies = [
    "openai>=1.0.0",
    "python-dotenv>=1.0.0",
]

[project.scripts]
buy-or-wait = "main:main"